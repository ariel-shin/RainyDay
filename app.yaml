runtime: python
api_version: 1
threadsafe: true

handlers:
- url: /stylesheets
  static_dir: stylesheets

- url: /
  script: main.app

- url: /images
  static_dir: images

- url: /upload
  script: main.app

env: flex

skip_files:
- env/
